upstream ${netloc} {
    % for address in addresses:
    server ${address};
    % endfor
}
 
server {

    access_log /var/log/nginx/access.log pancake;

    listen 80;
    server_name ${netloc};
    location / {
      proxy_pass ${url};
    }
}
